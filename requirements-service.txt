# The web service only. The CLI needs requirements.txt alone, so an upstream user
# designing guides never installs a web stack.
-r requirements.txt
fastapi >= 0.110
uvicorn[standard] >= 0.27
jinja2 >= 3.1
httpx >= 0.27        # starlette's TestClient, used by test/test_service_*.py
